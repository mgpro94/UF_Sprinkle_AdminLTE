<footer class="main-footer">
	<!-- To the right -->
	<div class="float-right d-none d-sm-inline">
		{{ translate("BUILT_WITH_UF") | raw }} &middot;
		<a href="{{site.uri.public}}/legal">{{ translate("TOS") }}</a> &middot;
		<a href="{{site.uri.public}}/privacy">{{ translate("PRIVACY") }}</a>
	</div>

	<!-- Default to the left -->
	&copy; <a href="{{site.uri.public}}">{{site.title}}</a>, {{ "now"|date("Y") }}. <span class="hidden-xs">{{ translate("ADMINLTE_THEME_BY") | raw }}.</span>
</footer>